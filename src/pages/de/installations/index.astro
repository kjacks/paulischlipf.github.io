---
import { getCollection } from 'astro:content';
import { useTranslations } from '../../../i18n/translations';
const t = useTranslations('de');

const installations = (await getCollection('installations', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{t('installations.title')}</title>
  </head>
  <body>
    <nav>
      <a href="/de">{t('nav.home')}</a>
      <a href="/de/works">{t('nav.works')}</a>
      <a href="/de/installations">{t('nav.installations')}</a>
      <a href="/installations" lang="en">English</a>
    </nav>

    <main>
      <h1>{t('installations.title')}</h1>
      <ul>
        {installations.map((installation) => (
          <li>
            <a href={`/de/installations/${installation.id}`}>
              <strong>{installation.data.title_de}</strong>
              {installation.data.location_de && <span> — {installation.data.location_de}</span>}
            </a>
            <p>{installation.data.description_de}</p>
            <time>{installation.data.date.toLocaleDateString('de-DE', { year: 'numeric', month: 'long' })}</time>
          </li>
        ))}
      </ul>
    </main>
  </body>
</html>
